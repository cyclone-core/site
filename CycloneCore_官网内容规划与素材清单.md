# Cyclone Core 官网内容规划与素材清单

> **状态**：官网暂缓建设，本文档为内容素材库 + 建站清单，随攒随更新。
> **整理日期**：2026-09-10
> **使用方式**：新素材随时追加到对应分区；建站时直接从「第一堆」开工。

---

## 1. 建站策略

- **暂缓建站，先攒内容**。内容成熟度已基本够单页官网，但发布主体未定（见 §6）。
- **建站触发条件**（满足其二即可开工）：

1. 博客素材攒够 5 篇以上；
2. 发布主体确定（个人 / 新公司），职务发明隔离已完成；
3. 检测机构 POC 进入实质对话，需要官网作名片。

---

## 2. 素材三分类总表

| 分类 | 处置 | 内容 |
| --- | --- | --- |
| 第一堆 | **直接上官网** | 四类测试地图、双模时钟、证据链、裕度分析、场景包、Bazel 集成、统计科普系列 |
| 第二堆 | **改造后放** | 跨行业方法学同构（去截图/公司名/薪酬）、Agent 评测扩展（仅作架构通用性一句） |
| 第三堆 | **永不公开** | JD 分析、薪酬行情、职业对冲、定价细节、客户排序、渠道返点、竞争判断 |

---

## 3. 第一堆：可直接上官网的素材

### 3.1 四类测试实现地图（产品页核心）★

**核心话术：一套引擎，四种姿势。** 四类测试共用场景 YAML + 调度器 + 判定引擎 + 证据链，变的是时钟模式、指标组合和故障配方。

| 测试类型 | 回答的问题 | 核心机制 | 判定指标 |
| --- | --- | --- | --- |
| 功能测试 | 该做的事做了吗 | 场景回放 + 行为断言 | recall、预警时机、误触发 = 0 |
| 性能测试 | 够不够快、稳不稳 | **在线模式** + 时间戳打点 | 时延 p95、超死线次数、抖动 |
| 可靠性测试 | 能一直对吗 | **seed 矩阵 + Wilson/SPRT** | 成功率下界、soak 时长 |
| 环境测试 | 恶劣条件下还正常吗 | **故障注入 = 环境应力的软件投影** | 退化曲线、闩锁违章 = 0 |

**功能测试**：离线模式 + 虚拟时钟，同用例重跑比特级一致；MVP 现有统计型指标判定，内核版规划单事件断言（"制动指令必须在 TTC < 1.2s 前发出"）。

**性能测试**：必须在线模式（虚拟时钟证明"算法走了几步"，证明不了"CPU 跑多快"）；IBusAdapter 入站/出站打硬件时间戳；总线突发注入加压。**边界**：CPU 占用/WCET 是 OS 级 profiler 的活，Cyclone Core 管总线可见的端到端时延。

**可靠性测试**：seed 矩阵跑 300 把，episode 成功率进 Wilson/SPRT（success-run testing：300/300 才能以 95% 置信声明 ≥ 99% 可靠性）；soak 长时测试盯内存泄漏/计数器溢出；故障战役输出退化曲线。**证据链价值最大的类型**：报告写"成功率 0.99（95% CI 下界，n=300，case hash 可复算）"。

**环境测试**：**诚实划界**——高低温/振动/EMC/盐雾（ISO 16750、GB/T 28046）是气候箱和台架的活，软件平台做不了物理环境试验。但环境应力最终投影在总线上：

| 物理应力 | 软件投影 | 注入方式 |
| --- | --- | --- |
| 高温 → 传感器漂移 | 测距偏置、噪声增大 | corrupt_field 偏置（场景包已有原型） |
| 振动 → 接插件松动 | 成簇丢帧 | 阵发性 drop（已有原型） |
| EMC | 位翻转、CRC 错误 | corrupt_field / corrupt_crc（规划中） |
| 电压跌落 → ECU 重启 | 节点冻结恢复 | freeze + restart（规划中） |

**卖法**：环境试验的软件伴随项——样件在箱子里烤，台架上同步跑故障战役，两本证据合订进 DV/PV 报告。

**四合一话术**：别家功能脚本、性能夹具、可靠性跑台、环境试验是四套班子四套报告；Cyclone Core 一套平台，四类报告同一格式、同一 hash 可验、同一 CI 门禁。

### 3.2 架构素材

- 双模时钟（虚拟钟 / 真实钟）与在线离线模式的 2×2 象限（详见《双模时钟与在线离线模式设计笔记》）；
- capture once, replay forever：实采一次，永久回放；
- C++ 内核 + Python 场景层的分层单引擎架构；进程边界 = YAML schema + 证据包格式。

### 3.3 证据链素材

- 四件套：case hash + JSONL SHA-256 digest + failure snapshot + JUnit XML；
- "同一故障场景重跑 N 次，判定结果与关键时序比特级一致"（POC 杀手级验收标准）；
- "PASS 不是运气，是地址"——content-addressed PASS（配合 Bazel remote cache 叙事）。

### 3.4 方法论素材：裕度分析三步法

1. 跑基线 → 得真实 SUT 指标基线（如近距召回 0.9931）；
2. 定裕度 → 阈值 = 基线 − 可接受退化量（收紧到 0.98）；
3. 双向校验 → 故障场景必须 FAIL，基线必须 PASS。

配套概念：健康裕度 / 检测裕度；可行性判据（基线 CI 下界 > 故障 CI 上界，否则该故障强度下不可检）；窗口相位（故障窗口 × 指标分箱 × 场景相位对齐）；剂量-响应曲线（扫故障强度 → 鲁棒性剖面 → 安全档案素材）。

### 3.5 场景包素材

- 22 个 ADAS 场景 YAML（主动安全 13 / 行车辅助 5 / 泊车 1 / 人机共驾 2 / L3 准入 1），全部实测判定符合预期；
- **留资钩子**：官网放目录，填邮箱发完整场景包；
- 标注：数值为演示数据，非真实 SUT 实测；阈值需按客户 SUT 基线标定（裕度分析服务项）。

### 3.6 开发者文档素材

- rules_cyclone：Bazel 三契约（exit code + `$XML_OUTPUT_FILE` + `$TEST_UNDECLARED_OUTPUTS_DIR`）；
- 硬件测试 tag 约定（manual / exclusive / local）；
- 三行接入示例（bazel_dep → register_toolchains → cyclone_test）。

### 3.7 博客选题库（内容营销金矿）

| # | 选题 | 状态 | 目标读者与 CTA |
| --- | --- | --- | --- |
| 1 | 为什么"跑 30 把过 27 把"不算验证通过——置信区间与点估计 | **草稿已完成**，见《博客草稿_01_跑30把过27把为什么不算验证通过.md》 | 测试经理；CTA：了解 verdict 引擎 |
| 2 | Wilson 下界判定：给通过率配一把诚实的尺子 | **草稿已完成**，见《博客草稿_02_Wilson下界判定_给通过率配一把诚实的尺子.md》 | 同上 |
| 3 | SPRT 与 LLR：证据攒够就提前下判的序贯检验 | **草稿已完成**，见《博客草稿_03_SPRT与LLR_证据攒够就提前下判.md》 | 测试工程师；CTA：试用场景包 |
| 4 | 裕度分析三步法：阈值不是拍脑袋，是可推导的 | **草稿已完成**，见《博客草稿_04_裕度分析三步法.md》 | 标定/测试工程师；CTA：预约 POC 咨询 |
| 5 | 双模时钟：离线可复现与在线真实感如何兼得 | **草稿已完成**，见《博客草稿_05_双模时钟.md》 | 架构师；CTA：下载场景包 |
| 6 | XiL 阶梯对照：从 ISS/cycleapprox/FPGA/HiL 到 MiL/SiL/PiL/HiL | **草稿已完成**，见《博客草稿_06_XiL阶梯对照.md》 | 跨行业工程师；CTA：关注专栏/转发 |
| 7 | 验证方法学的跨行业同构：芯片、AI Agent、汽车 | **草稿已完成（去敏感版）**，见《博客草稿_验证方法学的跨行业同构.md》 | 泛技术受众 |

---

## 4. 第二堆：改造后才能放

| 内容 | 改造要点 |
| --- | --- |
| 跨行业方法学同构（芯片 Emulation / AI 评测 / 汽车 XiL） | 去掉 JD 截图、公司名、薪酬数字；改写成纯方法学对比博客（选题 #7） |
| Agent 评测扩展 | 官网最多一句"同一内核可扩展至 AI Agent 评测"作架构通用性佐证；**不展开**，避免稀释汽车定位、引来错误客户 |

---

## 5. 第三堆：永不公开（内部战略）

- 三份 JD（Emulation / 腾讯云 Agent 评测 / AI 芯片测试&AI Infra）的截图与分析；
- 薪酬行情、职业对冲路径；
- 定价细节（POC 10–30 万、订阅 8–15 万/seat/年、买断 25–40 万 + 18%、runtime 2–3 万/node/年、渠道 65–70%）——官网只放「获取报价」；
- 客户排序（检测机构 > Tier2/具身初创 > OEM）与切入策略；
- 竞争判断（Agent 评测红海、芯片测试不碰等）。

---

## 6. 发布前检查清单

- [ ] **诚实标签**：全站沿用「已支持 / 原型验证中 / 规划中」三级标注——检测机构客户以审核为生，一处夸大的「已支持」被发现即信用归零；
- [ ] **演示数据标注**：MVP 数字（0.9931、0.98 等）一律标「演示数据，非真实 SUT 实测」；
- [ ] **发布主体**：确认以个人还是新公司名义上线，**与职务发明隔离是同一个问题，先定主体再上线**；
- [ ] 联系方式邮箱链接（mailto，主题预填「POC 咨询」），纯文本地址并列；
- [ ] 无客户 logo / 认证宣称（尚无客户与认证，一处都不许编）。

---

## 7. 素材积累规范

**素材卡格式**（每篇草稿头部三行）：

```javascript
目标读者：（如 检测机构测试主管）
看完做什么：（CTA，如 预约 POC 演示 / 下载场景包）
诚实标签：（涉及产品能力的句子是否都过了「已支持/原型/规划」检查）
```

**优先级队列**：统计判定系列（选题 1–4）> 双模时钟（5）> XiL 阶梯（6）> 跨行业同构（7）。

---

## 8. 官网 IA 草案（单页滚动，备建站用）

1. **首屏**：注入确定性，输出证据链 + 一句话副标 + 两个按钮（下载场景包 / 预约 POC）；
2. **痛点**：手动故障注入不可复现、报告缺证据链、判定靠拍脑袋阈值；
3. **四类测试地图**（§3.1 整表 + 四合一话术）；
4. **架构**：双模时钟、capture-replay 循环、C++/Python 分层；
5. **证据链四件套** + 杀手级验收标准；
6. **方法论**：裕度分析三步法；
7. **场景包目录**（留资钩子）；
8. **博客/方法论文章**列表；
9. **开发者**：Bazel 三行接入；
10. **商务**：「获取报价」+ 联系方式；
11. **页脚**：诚实标签说明、版本日期。